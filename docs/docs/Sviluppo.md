Per avviare l'ambiente di sviluppo, è necessario avere installato Docker.

Per avere tutti i container in esecuzione, e con refresh automatico, eseguire il comando:

```bash
# Solo la prima volta
make build-dev

make dev
```